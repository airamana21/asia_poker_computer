# Default: treat files as text and normalize line endings
* text=auto

# Shell scripts must be LF (macOS/Linux)
*.sh text eol=lf
*.bash text eol=lf

# PowerShell scripts are typically CRLF on Windows
*.ps1 text eol=crlf

# (Optional) If you have spec files / configs you want LF too
*.spec text eol=lf
